name: Build networkD3

on:
  workflow_dispatch:
  push:

jobs:
  build:
    name: Build networkD3
    permissions:
      checks: write
      contents: read
    uses: Displayr/nixr-public/.github/workflows/build-flake-installable.yaml@master
    with:
      installable-path: pkgs.x86_64-linux.rPackages.networkD3
      build-args: --override-input displayr-networkD3 .
      installable-name: networkD3
    secrets: inherit